<section id="zertifizierung" class="section-padding relative overflow-hidden">
  <div class="bg-glow" style="top: 20%; right: -10%; opacity: 0.1;"></div>
  
  <div class="container container-lux">
    <div class="trust-layout">
      <div class="trust-content reveal">
        <span class="section-label">Qualität & Sicherheit</span>
        <h2 class="section-title">Vertrauen durch<br />totale Transparenz</h2>
        <p class="trust-lead">
          Wir setzen auf Standards, die in der Medizintechnik keine Kompromisse zulassen. Jede Schiene ist ein Versprechen auf Präzision.
        </p>
        
        <div class="trust-checklist">
          <div class="check-item reveal delay-1">
            <div class="check-line"></div>
            <div class="check-text">
              <strong>100% Made in Germany</strong>
              <p>Von der digitalen Planung bis zum finalen Finish erfolgt jeder Schritt an unserem Standort.</p>
            </div>
          </div>
          
          <div class="check-item reveal delay-2">
            <div class="check-line"></div>
            <div class="check-text">
              <strong>ISO-zertifizierte Exzellenz</strong>
              <p>Höchste Fertigungsqualität nach strengen MDR-Richtlinien und ISO-Standards.</p>
            </div>
          </div>
          
          <div class="check-item reveal delay-3">
            <div class="check-line"></div>
            <div class="check-text">
              <strong>Direkter Experten-Kontakt</strong>
              <p>Keine Callcenter. Sie sprechen direkt mit den verantwortlichen Zahntechnikern.</p>
            </div>
          </div>
        </div>
      </div>
      
      <div class="trust-visual reveal delay-2">
        <div class="visual-container">
           <img src="/images/lab.png" alt="Modern Dental Lab Germany" class="lab-img" />
           <div class="glass-badge">
              <span class="badge-item">ISO 13485</span>
              <div class="badge-sep"></div>
              <span class="badge-item">MDR Ready</span>
           </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .container-lux {
    max-width: 1200px;
  }

  .trust-layout {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 8rem;
    align-items: center;
  }

  .trust-lead {
    font-size: 1.375rem;
    color: var(--color-primary);
    margin-bottom: 4rem;
    font-weight: 500;
    line-height: 1.5;
  }

  .trust-checklist {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .check-item {
    display: flex;
    gap: 2rem;
    position: relative;
  }

  .check-line {
    width: 2px;
    height: 100%;
    background: var(--color-border);
    position: absolute;
    left: 0;
    top: 0;
  }

  .check-line::after {
    content: '';
    position: absolute;
    top: 0;
    left: -2px;
    width: 6px;
    height: 6px;
    background: var(--color-accent);
    border-radius: 50%;
  }

  .check-text {
    padding-left: 1.5rem;
  }

  .check-text strong {
    display: block;
    font-size: 1.125rem;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
  }

  .check-text p {
    font-size: 0.9375rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .visual-container {
    position: relative;
    border-radius: var(--radius-lux);
    overflow: hidden;
    box-shadow: var(--shadow-lux);
  }

  .lab-img {
    width: 100%;
    display: block;
    transition: transform 0.8s cubic-bezier(0.19, 1, 0.22, 1);
  }

  .visual-container:hover .lab-img {
    transform: scale(1.05);
  }

  .glass-badge {
    position: absolute;
    bottom: 2rem;
    left: 2rem;
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    padding: 1rem 2rem;
    border-radius: 100px;
    display: flex;
    align-items: center;
    gap: 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.3);
    box-shadow: var(--shadow-lux);
  }

  .badge-item {
    font-size: 0.75rem;
    font-weight: 800;
    color: var(--color-primary);
    letter-spacing: 0.05em;
  }

  .badge-sep {
    width: 1px;
    height: 12px;
    background: var(--color-border);
  }

  @media (max-width: 1024px) {
    .trust-layout {
      grid-template-columns: 1fr;
      gap: 5rem;
    }
  }
</style>
