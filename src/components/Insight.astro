<section id="insight" class="insight-section section-padding">
  <div class="bg-glow" style="top: 50%; left: 50%; transform: translate(-50%, -50%); opacity: 0.15;"></div>
  
  <div class="container text-center reveal">
    <span class="section-label">Core Insight</span>
    <h2 class="insight-title">
      Der entscheidende Unterschied liegt nicht in der Schiene, <br />
      <span class="text-gradient">sondern in der Produktion dahinter.</span>
    </h2>
    <p class="insight-text reveal delay-1">
      Modern Clear bringt die Fertigung zurück nach Deutschland. Für absolute Transparenz, direkte Wege und die volle Kontrolle über jedes Detail Ihrer Planung.
    </p>
    <div class="insight-divider reveal delay-2"></div>
  </div>
</section>

<style>
  .insight-section {
    background: var(--color-primary);
    color: white;
    position: relative;
    overflow: hidden;
  }

  .insight-title {
    color: white;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.1;
    margin-bottom: 2.5rem;
    font-weight: 800;
  }

  .insight-text {
    color: rgba(255, 255, 255, 0.6);
    font-size: 1.25rem;
    max-width: 800px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .insight-divider {
    width: 60px;
    height: 4px;
    background: var(--color-accent);
    margin: 4rem auto 0;
  }
</style>
